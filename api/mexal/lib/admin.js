import { createClient } from "@supabase/supabase-js";
const ROLES = new Set(["admin", "administrator", "amministratore", "super admin", "direzione"]);
export function requireEnv(name) { const value = String(process.env[name] || "").trim(); if (!value) throw new Error(`Variabile Vercel mancante: ${name}`); return value; }
export function createAdminClient() { return createClient(requireEnv("SUPABASE_URL"), requireEnv("SUPABASE_SERVICE_ROLE_KEY"), { auth: { persistSession: false, autoRefreshToken: false } }); }
export async function requireAdmin(req, admin = createAdminClient()) { const match = String(req.headers?.authorization || "").match(/^bearer\s+(.+)$/i); if (!match) throw Object.assign(new Error("Sessione mancante."), { status: 401 }); const token = match[1]; const { data: { user }, error: authError } = await admin.auth.getUser(token); if (authError || !user) throw Object.assign(new Error("Sessione non valida."), { status: 401 }); const { data: profile, error } = await admin.from("utenti").select("id,attivo,ruoli(nome,livello)").eq("auth_user_id", user.id).maybeSingle(); if (error) throw Object.assign(error, { status: 500 }); const role = String(profile?.ruoli?.nome || "").trim().toLowerCase(); const allowed = profile?.attivo !== false && (Number(profile?.ruoli?.livello || 0) >= 80 || ROLES.has(role)); if (!allowed) throw Object.assign(new Error("Operazione riservata agli amministratori."), { status: 403 }); return { admin, token, user, profile }; }
