import { createAdminClient, requireAdmin } from "./lib/admin.js";
export default async function handler(req,res){if(req.method!=="POST")return res.status(405).json({error:"Metodo non consentito."});try{const admin=createAdminClient();await requireAdmin(req,admin);const id=req.body?.run_id;const {data,error}=await admin.from("mexal_automation_runs").update({status:"stopped",completed_at:new Date().toISOString(),current_action:null,error_message:"Automazione arrestata manualmente dall’amministratore."}).eq("id",id).in("status",["queued","running"]).select().maybeSingle();if(error)throw error;if(!data)return res.status(409).json({error:"La run non è arrestabile."});return res.status(200).json({run:data});}catch(error){return res.status(error.status||500).json({error:error.message});}}
